# kotlin 기본
+ Backing Fields
```kotlin
    class A{
        var count=0
            set(value){
                if(value>=0)filed=value
            }
        val isEmpty: Boolean
            get()=this.size==0
    }
```
